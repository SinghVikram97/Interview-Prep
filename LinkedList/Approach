1. For deletion use a previous pointer 
2. Finding mid use fast and slow pointer
3. Reversing the linked list using prev cur and next without recursion
4. Use recursion if same work repeated ex. pairwise swap nodes
5. Delete greater elements to right= Reverse the list + delete greater elements to left + reverse again
6. Palindrome=Reverse list from mid.
